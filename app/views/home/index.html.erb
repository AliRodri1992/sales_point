<%= render Landing::NavbarComponent.new %>

<main class="overflow-x-hidden">

  <%= render Landing::HeroComponent.new(
    badge: t('landing.hero.badge'),
    title: t('landing.hero.title'),
    highlighted: t('landing.hero.highlighted'),
    description: t('landing.hero.description'),
    image: "logo.png",
    primary_button: Ui::ButtonComponent.new(
      text: t('landing.buttons.start_free'),
      href: new_user_registration_path,
      icon: :rocket_launch,
      size: :lg,
      variant: :primary
    ),
    secondary_button: Ui::ButtonComponent.new(
      text: t('landing.buttons.view_demo'),
      href: "#demo",
      icon: :play_circle,
      size: :lg,
      variant: :outline
    ),
    statistics: [
      Ui::StatisticComponent.new(
        number: "+25K",
        label: t('landing.statistics.daily_sales'),
        icon: :shopping_cart
      ),
      Ui::StatisticComponent.new(
        number: "99.9%",
        label: t('landing.statistics.availability'),
        icon: :server
      ),
      Ui::StatisticComponent.new(
        number: "24/7",
        label: t('landing.statistics.support'),
        icon: :lifebuoy
      )
    ]
  ) %>

  <%= render Landing::FeaturesComponent.new(
    title: t('landing.features.title'),
    subtitle: t('landing.features.subtitle'),
    features: @features
  ) %>

  <%= render Landing::ModulesComponent.new(
    title: t('landing.modules.title'),
    subtitle: t('landing.modules.subtitle'),
    modules: @modules
  ) %>

  <%= render Landing::ScreenshotComponent.new(
    title: t('landing.screenshot.title'),
    description: t('landing.screenshot.description'),
    image: "logo.png"
  ) %>

  <section class="bg-slate-50 py-16 sm:py-24">

    <div class="mx-auto max-w-7xl px-4 sm:px-6">

      <%= render Ui::SectionTitleComponent.new(
        badge: t('landing.testimonials.badge'),
        title: t('landing.testimonials.title'),
        subtitle: t('landing.testimonials.subtitle')
      ) %>

      <div class="mt-10 grid gap-6 sm:mt-16 sm:gap-8 lg:grid-cols-3">

        <% @testimonials.each do |testimonial| %>

          <%= render Landing::TestimonialComponent.new(**testimonial.to_h) %>

        <% end %>

      </div>

    </div>

  </section>

  <section
    id="pricing"
    class="bg-white py-16 sm:py-28">

    <div class="mx-auto max-w-7xl px-4 sm:px-6">

      <%= render Ui::SectionTitleComponent.new(
        badge: t('landing.pricing.badge'),
        title: t('landing.pricing.title'),
        subtitle: t('landing.pricing.subtitle')
      ) %>

      <div class="mt-10 grid gap-6 sm:mt-16 sm:gap-8 lg:grid-cols-3">

        <% @plans.each do |plan| %>

          <%= render Landing::PricingCardComponent.new(**plan.to_h) %>

        <% end %>

      </div>

    </div>

  </section>

  <%= render Landing::FaqComponent.new(
    title: t('landing.faq.title'),
    subtitle: t('landing.faq.subtitle'),
    questions: @questions
  ) %>

  <%= render Landing::CtaComponent.new(
    title: t('landing.cta.title'),
    description: t('landing.cta.description'),
    button: Ui::ButtonComponent.new(
      text: t('landing.buttons.create_account'),
      href: new_user_registration_path,
      icon: :rocket_launch,
      size: :lg,
      variant: :secondary
    )
  ) %>

</main>

<%= render Landing::FooterComponent.new %>