<% area = @area %>
<div
  data-modal-target="panel"
  role="dialog"
  aria-modal="true"
  aria-labelledby="area-modal-title"
  class="relative w-full max-w-lg rounded-2xl border border-slate-200 bg-white shadow-xl"
>
  <div class="flex items-center justify-between border-b border-slate-100 px-6 py-4">
    <div>
      <h2 id="area-modal-title" class="font-jakarta text-lg font-bold text-slate-900">
        <%= area.persisted? ? t('admin.areas.edit.title', name: area.name) : t('admin.areas.new.title') %>
      </h2>
      <p class="mt-0.5 text-sm text-slate-500">
        <%= area.persisted? ? t('admin.areas.edit.subtitle') : t('admin.areas.new.subtitle') %>
      </p>
    </div>
    <button
      type="button"
      data-modal-dismiss
      data-action="modal#close"
      class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg text-slate-400 transition hover:bg-slate-100 hover:text-slate-600"
      aria-label="<%= t('admin.areas.show.close') %>"
    >
      <%= icon('x-mark', class: 'size-5') %>
    </button>
  </div>

  <div class="px-6 py-6">
    <%= render 'form', area: area %>
  </div>
</div>
